import Complete from "./complete";

const OrderComplete = () => {

    return (
        <div className="flex -mt-36 flex-col items-center justify-center min-h-screen">
            <Complete />
        </div>
    )
}

export default OrderComplete;